Задача 13: Задаем список из рандомных чисел (рандомной длины), нужно составить новый список только с уникальными значениями


import random

my_list = [random.randint(0,10) for _ in range(20)]
print(my_list)
unique_numbers = list(set(my_list))
print(unique_numbers)
